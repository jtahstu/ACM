#include<stdio.h>
#include<string.h>
int main()
{
   char str[12];
   int i,n,len,k[12];
   scanf("%d"&n);
   while(n--)
   {
       gets(str);
       len=strlen();
       for(i=1;i<=len;i++)
        if (a[i]==8)
        k[i]++;
   }
}
