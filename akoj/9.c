#include<stdio.h>
#include<string.h>
int main ()
{
    char str[1001];
    int n=0,i;

    while(1)
    {
        if(gets(str)==);
        n = strlen(str);
        for( i = 0; i <=n ; i++)
            printf("%c",str[i]);
        printf("\n");
    }

    return 0;
}
